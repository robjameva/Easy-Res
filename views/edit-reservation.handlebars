<div id="res-hours" class="row d-flex justify-content-center flex-wrap">
    {{!-- List of reservations.s --}}
    <div id="hours" class="col-lg-4 col-md-4 col-sm-8">
        <h2>Change Reservation</h2>
        <div>
            <form>
                <label for="party_size">Party Size:</label>
                <input type="number" id="party_size" min="1" max="100" value="{{reservations.party_size}}" />
            </form>
        </div>

        <div id="res-btns" class="d-flex flex-column gap-2">
            {{#each hoursObj as |hour|}}
            <button id="hour_{{@key}}" type="click" class="btn btn-light btn-sm" data-bs-toggle="modal"
                data-bs-target="#exampleModal">{{this}}</button>
            {{/each}}
            <button id="delete-button" class="delete-reservation-btn">Delete Reservation</button>
        </div>



    </div>

    {{!-- Restaurant info pane --}}
    <div id="restaurant-detail" class="col-lg-8 col-md-6 col-sm-6 d-flex justify-content-center align-items-center"
        style="background-image: url({{reservations.restaurant.business_image}})">
        <div class="info-wrapper">
            <div>
                <h1>{{reservations.restaurant.business_name}}</h1>
            </div>

            <div>
                <ul>
                    <li>Restaurant Information</li>
                    <li>{{reservations.restaurant.business_address}}</li>
                    <li>{{reservations.restaurant.business_phone}}</li>
                    <li><a href="{{reservations.restaurant.business_website}}"
                            target="_blank">{{reservations.restaurant.business_website}}</a>
                    </li>
                </ul>
            </div>
        </div>


    </div>
</div>


<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Reservation Changed</h5>
            </div>
            <div class="modal-body">
                Your reservation at {{reservations.restaurant.business_name}} is changed!
                <br>
                We sent a confirmation text to {{phoneNum}}
            </div>
            <div class="modal-footer">
                <button id="close" type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button id="main-menu" type="button" class="btn btn-primary">Main Menu</button>
            </div>
        </div>
    </div>
</div>



<script src="/javascript/availability.js"></script>
<script src="/javascript/edit-reservation.js"></script>
<script src="/javascript/delete-reservation.js"></script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
    crossorigin="anonymous"></script>